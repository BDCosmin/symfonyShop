{{ include ('default/parts/header.html.twig', { 'categories': categories|default([]) }) }}

{% block body %}
<div class="container-fluid">
   <div class="row d-xxl-flex d-xl-none d-lg-none d-md-none d-sm-none d-xs-none pt-2 pb-2">
      <div class="col-2"></div> {# Side space #}
      <div class="col-2">
         <div class="m-0 pt-3 pb-3 ps-2 bg-warning" style="border-radius: 4px">
            <h3>Categorii populare</h3>
            {% for category in twigGlobalsService.getMenuCategories() %}
               <a style="text-decoration: none; color: #1e1e1e" href="{{ url('category_view', {'category':category.id}) }}">
                  <b>{{ category.name }}</b>
               </a>
               <br/>
            {% endfor %}
         </div>
         <div class="m-0 p-0 ">
            <a href="{{ path('offer') }}">
               <img src="{{ asset('img/homepage-offer.png') }}" class="img-fluid homepageOfferSize" alt="homepage offer">
            </a>
         </div>
      </div> {# Categorii pop. #}
      <div class="col-6">
         <div id="carouselExample" class="carousel slide m-0"  data-bs-ride="carousel">
            <div class="carousel-inner">
               <div class="carousel-item active">
                  <img src="{{ asset('img/slide1.png') }}" class="d-block slideSize" alt="slide 1">
               </div>
               <div class="carousel-item">
                  <img src="{{ asset('img/slide2.png') }}" class="d-block slideSize" alt="slide 2">
               </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
               <span class="carousel-control-prev-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
               <span class="carousel-control-next-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Next</span>
            </button>
         </div>
      </div> {# Carousel #}
      <div class="col-2"></div> {# Side space #}
   </div>

   <div class="row d-xxl-none d-xl-flex d-lg-flex d-md-flex d-sm-none d-xs-none pt-2 pb-2">
      <div class="col-2"></div> {# Side space #}
      <div class="col-8">
         <div id="carouselExample" class="carousel slide m-0">
            <div class="carousel-inner">
               <div class="carousel-item active">
                  <img src="{{ asset('img/slide1.png') }}" class="d-block slideSize" alt="slide 1">
               </div>
               <div class="carousel-item">
                  <img src="{{ asset('img/slide2.png') }}" class="d-block slideSize" alt="slide 2">
               </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
               <span class="carousel-control-prev-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
               <span class="carousel-control-next-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Next</span>
            </button>
         </div>
      </div> {# Carousel #}
      <div class="col-2"></div> {# Side space #}
   </div>
   <div class="row d-xxl-none d-xl-none d-lg-none d-md-none d-sm-flex d-xs-flex pt-2 pb-2">
      <div class="col-12">
         <div id="carouselExample" class="carousel slide m-0">
            <div class="carousel-inner">
               <div class="carousel-item active">
                  <img src="{{ asset('img/slide1.png') }}" class="d-block slideSize" alt="slide 1">
               </div>
               <div class="carousel-item">
                  <img src="{{ asset('img/slide2.png') }}" class="d-block slideSize" alt="slide 2">
               </div>
            </div>
            <button class="carousel-control-prev" type="button" data-bs-target="#carouselExample" data-bs-slide="prev">
               <span class="carousel-control-prev-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#carouselExample" data-bs-slide="next">
               <span class="carousel-control-next-icon" aria-hidden="true"></span>
               <span class="visually-hidden">Next</span>
            </button>
         </div>
      </div> {# Carousel #}
   </div>

   <div class="row d-xxl-none d-xl-flex d-lg-flex d-md-flex d-sm-none d-xs-none pt-2 pb-2">
      <div class="col-2"></div> {# Side space #}
      <div class="col-6">
         <div class="m-0 pt-3 pb-3 ps-2 bg-warning" style="border-radius: 4px">
            <h3>Categorii populare</h3>
            {% for category in twigGlobalsService.getMenuCategories() %}
               <a style="text-decoration: none; color: #1e1e1e" href="{{ url('category_view', {'category':category.id}) }}">
                  <b>{{ category.name }}</b>
               </a>
               <br/>
            {% endfor %}
         </div>
      </div> {# Categorii pop. #}
      <div class="col-2 m-0 p-0">
         <a href="{{ path('offer') }}">
            <img src="{{ asset('img/homepage-offer.png') }}" class="img-fluid homepageOfferResize" alt="homepage offer">
         </a>
      </div> {# Offer ad #}
      <div class="col-2"></div> {# Side space #}
   </div>
   <div class="row d-xxl-none d-xl-none d-lg-none d-md-none d-sm-flex d-xs-flex pt-2 pb-2">
      <div class="col-8">
         <div class="m-0 pt-3 pb-3 ps-2 bg-warning" style="border-radius: 4px">
            <h3>Categorii populare</h3>
            {% for category in twigGlobalsService.getMenuCategories() %}
               <a style="text-decoration: none; color: #1e1e1e" href="{{ url('category_view', {'category':category.id}) }}">
                  <b>{{ category.name }}</b>
               </a>
               <br/>
            {% endfor %}
         </div>
      </div> {# Categorii pop. #}
      <div class="col-4 m-0 p-0">
         <a href="{{ path('offer') }}">
            <img src="{{ asset('img/homepage-offer.png') }}" class="img-fluid homepageOfferResize" alt="homepage offer">
         </a>
      </div> {# Offer ad #}
   </div>

   <div class="row d-xxl-flex d-xl-flex d-lg-flex d-md-flex d-sm-none d-xs-none pt-2 pb-2 mt-2">
      <div class="col-2"></div> {# Side space #}
      <div class="col-8">
         <div class="d-flex flex-column bg-warning-subtle p-4">
            <h4>Aboneaza-te la newsletter</h4>
            <br/>
            <p>Ramai la curent cu SoundVault! Primeste prin e-mail oferte exclusive la vinyl-uri, CD-uri si instrumente, plus cele mai recente sosiri, articole de colectie si echipamente muzicale. Primiti actualizari din interior, reduceri speciale si povesti muzicale livrate direct catre tine. Alatura-te comunitatii noastre de iubitori de muzica astazi!</p>
            <div class="d-flex flex-row">
               <input type="email" class="form-control me-2" id="newsletter" placeholder="Email">
               <button type="submit" class="btn btn-dark">Trimite</button>
            </div>
         </div>
      </div> {# Newsletter #}
      <div class="col-2"></div> {# Side space #}
   </div>
   <div class="row d-xxl-none d-xl-none d-lg-none d-md-none d-sm-flex d-xs-flex pt-2 pb-2 mt-2">
      <div class="col-12 ">
         <div class="d-flex flex-column bg-warning-subtle p-4">
            <h4>Aboneaza-te la newsletter</h4>
            <br/>
            <p>Ramai la curent cu SoundVault! Primeste prin e-mail oferte exclusive la vinyl-uri, CD-uri si instrumente, plus cele mai recente sosiri, articole de colectie si echipamente muzicale. Primiti actualizari din interior, reduceri speciale si povesti muzicale livrate direct catre tine. Alatura-te comunitatii noastre de iubitori de muzica astazi!</p>
            <div class="d-flex flex-row">
               <input type="email" class="form-control me-2" id="newsletter" placeholder="Email">
               <button type="submit" class="btn btn-dark">Trimite</button>
            </div>
         </div>
      </div> {# Newsletter #}
   </div>
</div>
{% endblock %}
{{ include ('default/parts/footer.html.twig') }}